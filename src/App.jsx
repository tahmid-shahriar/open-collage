import './App.css'

export default function App() {
  return (
    <div className="app-root">
      <header className="app-header">
        <h1>Open Collage</h1>
        <p className="tagline">Browser-only shell — editor ui lands in follow-up changes.</p>
      </header>
    </div>
  )
}
